# Railway configuration - Root of monorepo
# Este arquivo é usado quando o Root Directory do serviço está configurado como vazio (raiz)

# Para o serviço API, configure no Railway Dashboard:
# - Root Directory: (vazio)
# - Dockerfile Path: Dockerfile.api

# Para o serviço Dashboard, configure no Railway Dashboard:
# - Root Directory: (vazio)
# - Dockerfile Path: Dockerfile.dashboard

