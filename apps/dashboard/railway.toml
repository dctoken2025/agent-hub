[build]
builder = "dockerfile"
dockerfilePath = "apps/dashboard/Dockerfile"
watchPatterns = ["apps/dashboard/**", "packages/**"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

